const arreglo = ["texto", 125,true, {Propiedad:'valor'}];

const persona={
    nombre: "Alberto",
    edad:71,
    coche:{
        marca:"Clio",
        color:"azul",   
 },
}
//console.log(persona.coche.color);

const resultado = 7>1 ? 'El primer valor es mayor que el segundo' : 'El primer valor es menor que el segundo';
console.log(resultado);